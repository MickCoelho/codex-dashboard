{
  "name": "codex-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "python3 scripts/codex-dashboard.py",
    "serve": "node server.js",
    "dev": "yarn generate && yarn serve"
  }
}
